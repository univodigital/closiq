spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5433}/${DB_NAME:closiq}
spring.datasource.username=${DB_USER:closiq}
spring.datasource.password=${DB_PASSWORD:closiq}

spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}

logging.level.com.closiq=DEBUG
logging.level.org.springframework.security=INFO

closiq.otp.console-log-enabled=true
